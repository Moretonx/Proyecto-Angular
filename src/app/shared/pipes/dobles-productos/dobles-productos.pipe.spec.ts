import { DoblesProductosPipe } from './dobles-productos.pipe';

describe('DoblesProductosPipe', () => {
  it('create an instance', () => {
    const pipe = new DoblesProductosPipe();
    expect(pipe).toBeTruthy();
  });
});
